#include <bits/stdc++.h>

using namespace std;

int a, b, c, d, e;

int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);

  cin >> a >> b >> c >> d >> e;
  cout << a + b + c + d + e;

  return 0;
}
