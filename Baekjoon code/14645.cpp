#include <bits/stdc++.h>

using namespace std;

int N, K, A, B;

int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);

  cin >> N >> K;
  for (int i = 0; i < N; i++) cin >> A >> B;
  cout << "비와이";

  return 0;
}
